{% extends 'base.html.twig' %}

{% block title %}Editer Organisme{% endblock %}

{% block body %}
<div class="p-5">
    <h1>Editer Organisme</h1>

    {{ include('organisme/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('organisme/_delete_form.html.twig') }}
    <a href="{{ path('app_organisme_index') }}">Retourner à la liste des organismes</a>
</div>
{% endblock %}
