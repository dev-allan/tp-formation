{% extends 'base.html.twig' %}

{% block title %}Editer la promotion{% endblock %}

{% block body %}
<div class="p-5">
    <h1>Editer la promotion</h1>

    {{ include('promotion/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('promotion/_delete_form.html.twig') }}

    <a href="{{ path('app_promotion_index') }}">Retourner à la liste des promotions</a>
</div>
{% endblock %}
