<div class="mb-3 d-flex p-2">
{{ form_start(form) }}
<div class="d-flex flex-column p-5">
    {# {{ form_widget(form) }} #}
    Nom : {{ form_widget(form.nom) }}
    Date de début : {{ form_widget(form.date_debut) }}
    Date de fin : {{ form_widget(form.date_fin) }}
    Salle : {{ form_widget(form.accueille) }}
    Formateur : {{ form_widget(form.formateur_id) }}
    Promotion : {{ form_widget(form.promotion_id) }}
    <button class="btn btn-primary mt-3">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
</div>
